plugins {
    id 'java-library'
}

dependencies {
    api project(':vocabulary')
    api project(':application:domain')
    api project(':application:queries')

    implementation 'jakarta.inject:jakarta.inject-api'

    testImplementation project(':testing:doubles')
}